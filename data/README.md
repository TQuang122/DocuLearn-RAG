Data Directory

The data/ directory contains the input PDF documents. These files serve as the system’s data source and are processed by extracting their content, splitting it into chunks, and indexing those chunks in the vector store.